# AI 协作者守则

你在这个仓库里写代码时，面对的是一位不靠手写工程来做游戏的所有者。他要能读懂你的 PR，并在 Godot 里看见你改的界面。

## 必须遵守

- 不要直接推送到 `main`。从 `main` 建分支，开 PR 合回去。
- 分支名用 `feature/`、`fix/`、`chore/` 或 `docs/`。
- PR 保持小，一件事一个 PR。
- PR 标题用英文 Conventional Commits。PR 正文用简体中文，写清改了什么、为什么改、怎么验证。
- 给人看的文档、PR 描述和代码注释使用简体中文。代码标识符、文件名和提交说明保持英文。
- 同一次 PR 里更新受影响的文档、`docs/GDD.md`（玩法有变时）和 `CHANGELOG.md`。
- 为行为变化添加或更新测试，并让 `lint` 和 `test` 能通过。
- 改了界面就在 PR 里放截图。
- 遵循 `docs/CODING_STYLE.md`。
- 重要的技术选择写成 `docs/adr/` 里的新记录，不要只留在对话里。
- 数值放进 `data/balance/`，不要写死在场景脚本里。
- 不要提交 keystore、密码、令牌或 `.env`。调试证书由 CI 临时生成。
- 不要手改 `addons/gut/`。
- 不要把多人、服务器或账号系统提前加进来。那是路线图后段，而且要先有新的 ADR。
- Godot 版本以 `godot-version.txt` 为准。要升级就同时改文档、CI 和一篇 ADR。

## 玩法边界

`docs/GDD.md` 里标成「待你补充」的内容，不要替所有者编成正式设定。可以留下明显的占位，并在 PR 里请他填写。
